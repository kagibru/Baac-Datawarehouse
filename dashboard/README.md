# Tableau de bord

La restitution reproductible sera réalisée dans Metabase Community, connecté au schéma `dw` de PostgreSQL.

Le modèle restera également compatible avec Power BI via le connecteur PostgreSQL.

